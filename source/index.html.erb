  <!-- Banner Section -->
<%= partial 'partials/banner' %>
<div class="container mb-5">
  <!-- About Section -->
  <%= partial 'partials/about' %>
  <!-- Projects Section -->
  <%= partial 'partials/projects' %>
  <!-- TechStack Section -->
  <%= partial 'partials/techStack' %>
  <!-- Social Links Section -->
  <%= partial 'partials/social' %>
</div>
