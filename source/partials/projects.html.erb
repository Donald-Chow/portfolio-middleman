<div class="row nav-group" id="projects">
  <div class="col-12">
      <h2> Projects</h2>
      <div class="cards mt-3">
        <% data.projects.each do |project| %>
          <div class="card-project" style="border-left: 3px solid <%= project.color %>;">
            <%= image_tag project.image, alt: "project image" %>
            <div class="card-project-outer">
              <div class="card-project-year">
                <%= project.year %>
              </div>
              <div class="card-project-infos my-2">
                <div class="d-flex justify-content-between align-items-center">
                  <h2><%= project.name %></h2>
                  <% if project.url.present? %>
                    <% link_to project.url, class: 'external-link', target: '_blank' do %>
                      <div style="color: <%= project.color %>">
                        <i class="fas fa-external-link-alt"></i> Link
                      </div>
                    <% end %>
                  <% end %>
                </div>
                <!-- project description -->
                <p class="mb-1 description-primary"><%= project.description %></p>
                <p class="mb-1 description-optional"><%= project.description2 %></p>
                <p class="mb-1 description-optional"><%= project.description3 %></p>

                <!-- stack icons -->
                <% project.stack.each do |tech| %>
                  <% data.icons.each do |category| %>
                    <% category['elements'].each do |element| %>
                      <% if element['name'] == tech %>
                        <%= element.plain %>
                      <% end %>
                    <% end %>
                  <% end %>
                <% end %>

              </div>
            </div>
          </div>
        <% end %>
      </div>
  </div>
</div>
